import {
  StyleSheet
} from 'react-native';
export const platform = StyleSheet.create({
    header:{
        margin:0,
        backgroundColor: '#FFFFFF',
        minHeight: 45,
        display: "flex",
        flexDirection: 'row',
        justifyContent:"center",
        alignItems:"center",

    },
});
